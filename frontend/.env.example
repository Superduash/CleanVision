# Base URL of the deployed Flask backend on Render.
# Leave empty in local development — the Vite proxy handles /api/* → localhost:5000.
# In production on Vercel, set this to: https://your-api.onrender.com
VITE_API_BASE_URL=
